---
- include: locale.yml
  tags:
    - locale

- include: packages.yml
  tags:
    - packages

- include: kernel.yml
  tags:
    - kernel

- include: network.yml
  tags:
    - network

- include: journal.yml
  tags:
    - journal

- include: filesystem.yml
  tags:
    - filesystem

- include: mounts.yml
  tags:
    - mounts

- include: disks.yml
  tags:
    - disks

- include: ramdisk.yml
  tags:
    - ramdisks

- include: security.yml
  tags:
    - security

- include: unattended-upgrades.yml
  tags:
    - unattended-upgrades

- include: user.yml
  tags:
    - user
