apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: postgres-backup
  namespace: backup-system
spec:
  # Backup the database every day at 2AM
  schedule: "01 00 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: postgres-backup
              image: postgres:12
              command: ["/bin/sh"]
              args:
                [
                  "-c",
                  'pg_dump -U benji -h ${SECRET_POSTGRES_HOST} benji > /var/backups/benji/backup-$(date +"%m-%d-%Y-%H-%M").sql',
                  '&& curl -fsS -m 10 --retry 5 -o /dev/null https://healthchecks.k8s.aml.ink/ping/73ec5213-d30b-4cc3-9ec2-1c2436ce0401'
                ]
              env:
                - name: PGPASSWORD
                  value: ${SECRET_BENJI_POSTGRES_PASSWORD}
              volumeMounts:
                - mountPath: /var/backups
                  name: postgres-storage
          restartPolicy: Never
          volumes:
            - name: postgres-storage
              persistentVolumeClaim:
                claimName: backup-system-nfs-tank-share-pvc
