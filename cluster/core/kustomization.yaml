apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - kube-system
  - rbac
  #- networking   # Moved to own step in base
  #- velero       # Moved to own step in base
  #- vpn-gateway  # Moved to own step in base
  - security
  - resource-control
  - system-upgrade
  - monitoring

