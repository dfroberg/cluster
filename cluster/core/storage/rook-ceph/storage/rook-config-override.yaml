apiVersion: v1
kind: ConfigMap
metadata:
  name: rook-config-override
  namespace: rook-ceph
data:
  config: |
    [global]
    # osd crush update on start = false
    # osd pool default size = 2
    # mon lease = 30.0