apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Provider
metadata:
  name: slack
  namespace: flux-system
spec:
  type: slack
  channel: managed-services
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  secretRef:
    name: slack-url 