apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mysql-operator
  namespace: wpops
spec:
  interval: 1m
  chart:
    spec:
      chart: wordpress-site
      version: 0.6.2
      sourceRef:
        kind: HelmRepository
        name: bitpoke-charts
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values: