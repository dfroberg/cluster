apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: amn
  namespace: flux-system
spec:
  interval: 1m
  url: https://gitlab.froberg.org/dfroberg/wp-k8s.git
  secretRef:
      name: gitlab-https-credentials
  ref:
    branch: main


