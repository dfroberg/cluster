apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cluster-sops-keys/cluster-secrets-repo.taml
  - cluster-sops-keys/cluster-secrets.sops.taml
patchesStrategicMerge:
  - ./init-patches.yaml  
