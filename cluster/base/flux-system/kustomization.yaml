apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - gotk-components.yaml
  - gotk-sync.yaml
  - cluster-sops-keys/kustomization.yaml
  - charts
patchesStrategicMerge:
  - ./gotk-patches.yaml
